# dependencies
node_modules/

# npm publish artifacts
*.tgz

# local secrets — never commit (credentials live in user-level ~/.npmrc)
.env
.env.*

# local verification / scratch
.verify/
*.log

# screenshots dropped into the project root
Snipaste_*
